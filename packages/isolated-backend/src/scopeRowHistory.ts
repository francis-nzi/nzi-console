import type { AuditEventReadModel } from "./readModels";
import type { Queryable } from "./postgres";

export async function listScopeRowAuditEvents(db:Queryable,jobId:string,rowId:string,limit=50):Promise<AuditEventReadModel[]>{const safeLimit=Math.min(Math.max(Math.trunc(limit),1),100),{rows}=await db.query<{audit_event_id:string;occurred_at:Date|string;actor_id:string;principal_type:AuditEventReadModel["principal"];organisation_id:string;action:string;entity_type:string;entity_id:string;correlation_id:string;reason:string|null;before_json:unknown;after_json:unknown}>(`SELECT a.audit_event_id,a.occurred_at,a.actor_id,a.principal_type,a.organisation_id,a.action,a.entity_type,a.entity_id,a.correlation_id,a.reason,a.before_json,a.after_json FROM nzi_console.audit_events a JOIN nzi_console.job_scope_rows r ON (r.organisation_id,r.scope_row_id)=(a.organisation_id,a.entity_id) WHERE r.job_id=$1 AND r.scope_row_id=$2 AND a.entity_type='scope_row' ORDER BY a.occurred_at DESC,a.audit_event_id DESC LIMIT $3`,[jobId,rowId,safeLimit]);const display=(value:unknown)=>value==null?undefined:typeof value==="string"?value:JSON.stringify(value);return rows.map(row=>({id:row.audit_event_id,at:row.occurred_at instanceof Date?row.occurred_at.toISOString():String(row.occurred_at),actor:row.actor_id,principal:row.principal_type,organisation:row.organisation_id,action:row.action,entity:row.entity_type,entityId:row.entity_id,result:"allowed",severity:row.reason?"warning":"info",correlationId:row.correlation_id,...(display(row.before_json)?{before:display(row.before_json)}:{}),...(display(row.after_json)?{after:display(row.after_json)}:{}),...(row.reason?{reason:row.reason}:{})}));}
