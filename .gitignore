node_modules
.pnpm-store
.next
out
dist
.turbo
coverage
*.tsbuildinfo
.env
.env.*
!.env.example
.DS_Store

# Local-only reference copy of the live platform and scratch space — never committed
nzi-live-fix/
_to_delete/

# Playwright rendered-acceptance artifacts
test-results/
playwright-report/
blob-report/
playwright/.cache/

# Scratch copies of handoff briefs. Untracked working notes — and they have carried
# credential values in the past, which must never reach the repository.
Claude outputs/
