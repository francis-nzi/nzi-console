import "server-only";

const escape=(value:string)=>value.replace(/\\/g,"\\\\").replace(/\(/g,"\\(").replace(/\)/g,"\\)").replace(/[^\x20-\x7e]/g,"?");
function wrap(value:string,width=88){const words=value.split(/\s+/),lines:string[]=[];let line="";for(const word of words){if(`${line} ${word}`.trim().length>width){if(line)lines.push(line);line=word}else line=`${line} ${word}`.trim()}if(line)lines.push(line);return lines;}
export function textPdf(title:string,sections:Array<{heading:string;lines:string[]}>){const content:string[]=["BT","/F1 18 Tf","54 770 Td",`(${escape(title)}) Tj`,`0 -28 Td`,`/F1 10 Tf`];for(const section of sections){content.push(`(${escape(section.heading)}) Tj`,`0 -17 Td`,`/F1 9 Tf`);for(const line of section.lines.flatMap(item=>wrap(item))){content.push(`(${escape(line)}) Tj`,`0 -13 Td`)}content.push("0 -8 Td","/F1 10 Tf")}content.push("ET");const stream=content.join("\n"),objects=["<< /Type /Catalog /Pages 2 0 R >>","<< /Type /Pages /Kids [3 0 R] /Count 1 >>","<< /Type /Page /Parent 2 0 R /MediaBox [0 0 612 792] /Resources << /Font << /F1 5 0 R >> >> /Contents 4 0 R >>",`<< /Length ${Buffer.byteLength(stream)} >>\nstream\n${stream}\nendstream`,"<< /Type /Font /Subtype /Type1 /BaseFont /Helvetica >>"];let pdf="%PDF-1.4\n",offsets=[0];objects.forEach((object,index)=>{offsets.push(Buffer.byteLength(pdf));pdf+=`${index+1} 0 obj\n${object}\nendobj\n`});const xref=Buffer.byteLength(pdf);pdf+=`xref\n0 ${objects.length+1}\n0000000000 65535 f \n${offsets.slice(1).map(offset=>`${String(offset).padStart(10,"0")} 00000 n `).join("\n")}\ntrailer\n<< /Size ${objects.length+1} /Root 1 0 R >>\nstartxref\n${xref}\n%%EOF`;return Buffer.from(pdf);}
