import Link from "next/link";
import {notFound} from "next/navigation";
import {AppShell,TopBar,WorkspaceRail} from "@nzi/ui";
import {clients as fixtureClients,jobs as fixtureJobs,clientStatusMeta,jobFamilyMeta} from "@nzi/mock-data";
import type {ClientScreenReadModel,JobScreenReadModel} from "@nzi/isolated-backend";
import {loadScreen} from "../../lib/loadScreen";
import {ScreenState} from "../../lib/ScreenState";
import {NAV,USER} from "../../lib/nav";

export const dynamic="force-dynamic";
export default async function ClientPage({params}:{params:Promise<{clientId:string}>}){const {clientId}=await params,[clientResult,jobResult]=await Promise.all([loadScreen<{clients:ClientScreenReadModel[]}>("clients",{clients:fixtureClients}),loadScreen<{jobs:JobScreenReadModel[]}>("jobs",{jobs:fixtureJobs})]);return <ScreenState result={clientResult}>{clientData=>{const client=clientData.clients.find(item=>item.id===clientId);if(!client)notFound();return <ScreenState result={jobResult}>{jobData=><ClientWorkspace client={client} jobs={jobData.jobs.filter(job=>job.header.clientId===client.id)}/>}</ScreenState>}}</ScreenState>}

function ClientWorkspace({client,jobs}:{client:ClientScreenReadModel;jobs:JobScreenReadModel[]}){const meta=clientStatusMeta[client.status],open=jobs.filter(job=>["draft","open","on-hold"].includes(job.header.status)),crp=jobs.filter(job=>job.header.family==="crp"),reviewGaps=crp.reduce((sum,job)=>sum+(job.detail.kind==="crp"?Math.max(0,job.detail.totalRows-job.detail.reviewedRows):0),0);return <AppShell rail={<WorkspaceRail sections={NAV} activeId="clients" user={USER}/>}><TopBar searchPlaceholder="Search this client…" crumbs={<><Link href="/clients">Clients</Link><span className="muted">/</span><b>{client.name}</b></>}/><div className="nz-head"><div className="nz-client-head"><div className="nz-client-monogram">{client.name.split(/\s+/).slice(0,2).map(word=>word[0]).join("")}</div><div className="nz-client-identity"><div><h1>{client.name}</h1><span className={`nz-st ${meta.cls}`}>{meta.label}</span></div><p className="sub">{client.sector} · {client.location} · Account owner {client.owner}</p></div><div className="nz-head-actions"><Link className="nz-btn" href="/platform">Portal access</Link><Link className="nz-btn pri" href={`/jobs?client=${client.id}`}>Create job</Link></div></div></div><div className="nz-body" style={{paddingTop:16}}><div className="nz-metrics"><Metric label="Open jobs" value={String(open.length)} note={`${jobs.length} total engagements`}/><Metric label="Data completeness" value={`${client.completeness}%`} note={client.completeness>=80?"Portfolio data on track":"Attention recommended"}/><Metric label="Rows awaiting review" value={String(reviewGaps)} note="Across CRP engagements"/><Metric label="Next report" value={client.nextReportDue||"Not scheduled"} note="Client reporting calendar"/></div><div className="nz-client-grid"><div className="nz-client-main"><section className="nz-panel"><Head title="Engagements" right={<Link href={`/jobs?client=${client.id}`}>New job →</Link>}/>{jobs.length===0?<Empty text="No engagements have been created for this client."/>:<table className="nz-tbl"><thead><tr><th>Job</th><th>Family</th><th>Stage</th><th>Progress</th><th>Owner</th><th>Due</th></tr></thead><tbody>{jobs.map(job=><tr key={job.header.id}><td><Link href={`/jobs/${job.header.id}`} style={{fontWeight:650,textDecoration:"none"}}>{job.header.number}</Link><div className="muted" style={{fontSize:11,marginTop:2}}>{job.header.title}</div></td><td><span className="nz-st need">{jobFamilyMeta[job.header.family].code}</span></td><td>{job.header.workflowStage}</td><td><Progress value={job.header.progressPct}/></td><td>{job.header.owner}</td><td className="num">{job.header.dueDate}</td></tr>)}</tbody></table>}</section><section className="nz-panel"><Head title="Reporting and assurance"/><div className="nz-client-signals"><Signal label="CRP engagements" value={String(crp.length)} tone="ok"/><Signal label="Review outstanding" value={String(reviewGaps)} tone={reviewGaps?"warn":"ok"}/><Signal label="Latest footprint" value={client.latestFootprint??"Not reported"} tone={client.latestFootprint?"ok":"warn"}/></div></section></div><aside className="nz-client-aside"><section className="nz-panel"><Head title="Primary contact"/><div className="nz-client-contact"><b>{client.contact.name||"Not configured"}</b><div className="sub">{client.contact.role||"Role not configured"}</div>{client.contact.email?<a href={`mailto:${client.contact.email}`}>{client.contact.email}</a>:null}</div></section><section className="nz-panel"><Head title="Account overview"/><div style={{padding:"6px 16px 12px"}}><Row label="Member since" value={client.memberSince}/><Row label="Account owner" value={client.owner}/><Row label="Year-on-year" value={client.yoy??"Not available"}/><Row label="Open jobs" value={String(client.openJobs)}/></div></section><section className="nz-panel nz-client-experience"><div className="eyebrow">Client experience</div><h2>Portal and reports</h2><p>Manage account invitations and job-level access, then review published reports from the same evidence base.</p><div><Link href="/platform" className="nz-btn pri">Manage access</Link><Link href="/reports" className="nz-btn">Reports</Link></div></section></aside></div></div></AppShell>}

function Head({title,right}:{title:string;right?:React.ReactNode}){return <div style={{display:"flex",padding:"13px 16px",borderBottom:"1px solid var(--line2)",alignItems:"center"}}><h2 style={{fontSize:15,margin:0}}>{title}</h2>{right?<div className="sub" style={{marginLeft:"auto",fontSize:12}}>{right}</div>:null}</div>}
function Metric({label,value,note}:{label:string;value:string;note:string}){return <div className="nz-metric"><div className="l">{label}</div><div className="v num">{value}</div><div className="sub" style={{fontSize:11,marginTop:3}}>{note}</div></div>}
function Progress({value}:{value:number}){return <span className="nz-client-progress"><span role="progressbar" aria-label="Job progress" aria-valuenow={value} aria-valuemin={0} aria-valuemax={100}><span style={{width:`${value}%`}}/></span><span className="num muted">{value}%</span></span>}
function Signal({label,value,tone}:{label:string;value:string;tone:"ok"|"warn"}){return <div style={{border:"1px solid var(--line)",borderRadius:10,padding:13}}><span className={`nz-st ${tone==="ok"?"done":"est"}`}>{tone==="ok"?"On track":"Attention"}</span><div style={{fontSize:18,fontWeight:650,marginTop:9}}>{value}</div><div className="sub" style={{fontSize:11,marginTop:3}}>{label}</div></div>}
function Row({label,value}:{label:string;value:string}){return <div className="nz-kv"><span className="k">{label}</span><span className="v">{value}</span></div>}
function Empty({text}:{text:string}){return <div className="sub" style={{padding:18}}>{text}</div>}
