import {PortalInviteSetup} from "./PortalInviteSetup";
export default async function PortalInvitePage({searchParams}:{searchParams:Promise<{token?:string}>}){const {token=""}=await searchParams;return <main className="nz-invite-shell"><header className="nz-account-header"><a href="/portal/login" className="nz-portal-brand"><span>N</span><div><b>NZI Pro</b><small>Client portal</small></div></a><span className="nz-invite-secure">Secure account enrolment</span></header><section className="nz-invite-layout"><aside className="nz-invite-story"><span className="nz-eyebrow light">Welcome to NZI Pro</span><h1>Your reporting workspace starts with trusted access.</h1><p>Create a dedicated portal password and enrol MFA before viewing client data or published reports.</p><ol><li><b>1</b><span><strong>Create password</strong><small>At least 12 unique characters</small></span></li><li><b>2</b><span><strong>Connect authenticator</strong><small>Protect every future sign-in</small></span></li><li><b>3</b><span><strong>Enter your workspace</strong><small>Access authorised reporting jobs</small></span></li></ol></aside><PortalInviteSetup token={token}/></section></main>}
