import {createPortalInvitation,createPortalRecoveryInvitation,PortalInvitationError} from "@nzi/isolated-backend";
import {isolatedPool} from "../../../lib/isolatedDatabase";
import {currentStaff,requireAuthOrigin} from "../../../lib/staffSession";
import {authFailure} from "../../../lib/authResponse";
export async function POST(request:Request){try{if(process.env.NZI_WRITE_API_ENABLED!=="true")return Response.json({code:"WRITE_API_DISABLED",message:"Write API is disabled."},{status:503});requireAuthOrigin(request);const principal=await currentStaff(request),body=await request.json() as {clientId?:unknown;email?:unknown;displayName?:unknown;recoveryPortalUserId?:unknown};if(typeof body.recoveryPortalUserId==="string"){const invitation=await createPortalRecoveryInvitation(isolatedPool(),principal,{portalUserId:body.recoveryPortalUserId});return Response.json({invitation},{status:201,headers:{"Cache-Control":"no-store"}});}if(typeof body.clientId!=="string"||typeof body.email!=="string"||typeof body.displayName!=="string")return Response.json({code:"INVALID_INVITATION",message:"A client, email and display name are required."},{status:422});const invitation=await createPortalInvitation(isolatedPool(),principal,{clientId:body.clientId,email:body.email,displayName:body.displayName});return Response.json({invitation},{status:201,headers:{"Cache-Control":"no-store"}});}catch(error){if(error instanceof PortalInvitationError)return Response.json({code:"INVALID_INVITATION",message:error.message},{status:422});return authFailure(error);}}
